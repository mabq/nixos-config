# .desktop files

Plain-text configuration files that describe an application (or a link, or a directory) so that a graphical desktop environment can display it in menus, launchers, docks, and file managers — without needing to know anything about how the app is actually installed or run.

They're defined by the freedesktop.org Desktop Entry Specification, so they're a cross-desktop standard, not tied to any one DE.
